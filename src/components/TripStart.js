import React from 'react';
import TripOrderForm from '../containers/TripOrderForm';

const TripStart = () => {
  return (
    <div className="trip-start--container">
      <TripOrderForm />
      Trip Start Container
    </div>
  );
};

export default TripStart;
